<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LegoMobile.Collections.AddSetToCollection.CollectionLookUpSet">
    <ContentPage.Content>
        <StackLayout
            VerticalOptions="CenterAndExpand"
                HorizontalOptions="Center"
                WidthRequest="300">
            <Label Text="Look Up Set"
               FontAttributes="Bold"
               FontSize="40"
               HorizontalOptions="Center"
               VerticalOptions="CenterAndExpand"
               Margin="0,0,0,100">
            </Label>
            <Entry    
            Placeholder="Enter Set Number  " HorizontalOptions="CenterAndExpand"
            VerticalOptions="CenterAndExpand"
            TextColor="Black"
            FontSize="26"
            Margin="0,0,0,0">
            </Entry>
            <Button    
            CornerRadius="25"
            Text="Fetch "
            BackgroundColor="#035a91" 
            TextColor="Black"
            Margin="0,75,0,25"
            x:Name="CollectionFetchAPIButton"
            Clicked="CollectionFetchAPIButton_Clicked">
            </Button>
            <Button    
            CornerRadius="25"
            Text="Add Manually"
            BackgroundColor="#035a91" 
            TextColor="Black"
            Margin="0,0,0,25"
                x:Name="CollectionAddManuallyButton"
                Clicked="CollectionAddManuallyButton_Clicked">
            </Button>
            <Button    
            CornerRadius="25"
            Text="Scan Barcode"
            BackgroundColor="#035a91" 
            TextColor="Black"
            Margin="0,0,0,0"
            x:Name="CollectionScanBarcodeButton"
                Clicked="CollectionScanBarcodeButton_Clicked">
            </Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>